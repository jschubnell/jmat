from setuptools import setup

setup(name='jmat',
      version='0.2',
      description='A package to work with matrix operations',
      author='João Schubnell',
      author_email='jschubnell@protonmail.com',
      packages=['jmat'],
      zip_safe=False)
